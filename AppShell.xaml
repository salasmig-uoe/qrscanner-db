<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="QRScanner.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:QRScanner.Pages"
    Shell.FlyoutBehavior="Flyout"
    Title="QRScanner">
    <Shell.FlyoutHeader>
        <Grid HeightRequest="100" BackgroundColor="DarkSlateBlue">
            <!-- <Image Source="moon.png"/> -->
        </Grid>
    </Shell.FlyoutHeader>
    <FlyoutItem Title="Art Scanner">
        <ShellContent Title="Sale"
                ContentTemplate="{DataTemplate local:SellingRecordPage}" />
        <ShellContent Title="Scan"
                ContentTemplate="{DataTemplate local:MainScanPage}" />
        <ShellContent Title="View"
            ContentTemplate="{DataTemplate local:ViewRecordsPage}" />
        <ShellContent Title="Sale Details"
            ContentTemplate="{DataTemplate local:DetailViewPage}" />
    </FlyoutItem>
</Shell>
